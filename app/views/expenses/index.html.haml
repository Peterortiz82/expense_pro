-if params[:q] && @expenses.empty?
  %h4 Sorry, but we could not find any results that matched your criteria. Please try your search again.
  = link_to 'Back', :back
-else
  - if Feature.active?(:search)
    .col-md-1-pull-right
    = button_tag 'Search', id: 'search-button', class: 'btn btn-default' do
      %span.glyphicon.glyphicon-search
  = render 'shared/expense_search'
  = render 'shared/expenses'